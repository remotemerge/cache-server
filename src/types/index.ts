export type HeadlessConfig = {
  url: string;
  userAgent: string;
  wait: number;
  headless: boolean;
};

export type CliConfig = {
  host: string;
  port: number;
  wait: number;
  headless: boolean;
};
